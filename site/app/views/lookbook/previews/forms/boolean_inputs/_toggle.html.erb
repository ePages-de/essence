<%= simple_fields_for nil do |f| %>
  <%= f.input :toggle,
              as: :toggle,
              required:,
              label: label ? nil : false,
              disabled:,
              error: error ? t('simple_form.errors.toggle') : nil %>
<% end %>
