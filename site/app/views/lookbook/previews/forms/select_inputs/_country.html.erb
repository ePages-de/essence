<%= simple_fields_for nil do |f| %>
  <%= f.input :country,
              as: :country,
              required:,
              label: label ? nil : false,
              hint: hint ? nil : false,
              prompt: prompt ? :translate : false,
              disabled:,
              error: error ? t('simple_form.errors.country') : nil,
              selected: error ? t('simple_form.values.country') : nil %>
<% end %>
